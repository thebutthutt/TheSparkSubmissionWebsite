<ul class="list-group">
    <% for (var i = 0; i < filenames.length; i++) { %>
    <li class="list-group-item"><%= filenames[i].split('sparkdelim').pop();%>
        <button class="btn btn-danger btn-delete" fileID='<%=fileIDs[i]%>'>Delete</button>
    </li>
    <% } %>
</ul>